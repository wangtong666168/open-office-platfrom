server:
  port: 7000
spring:
  application:
    name: user-center
  cloud:
    config:
      label: master
      uri: http://localhost:9090
      name: application
      profile: dev

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always  
